# Rust/Anchor
/target/
.anchor/
**/*.so
**/*.dSYM

# Node/TypeScript
/node_modules/
/dist/
npm-debug.log*
# keep lockfiles tracked; comment the next line if you really want to ignore
# package-lock.json

# Solana keys (keep deploy keys if needed in repo policy)
**/*.keypair
*.key
*.pem

# OS/editor
.DS_Store
.vscode/
/.idea/
*.swp

# Web build output
/app/web/dist/

